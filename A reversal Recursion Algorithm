def reverse(strr):
   start_index = 0
   last_index = len(strr) - 1
   reversed_string = ""

   while last_index >= start_index:
       reversed_string += strr[last_index]
       last_index -= 1

   return reversed_string



print(reverse("straw"))
